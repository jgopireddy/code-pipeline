version: 0.2

phases:
  install:
    commands:
      - npm install -g npm
      - npm install
  build:
    commands:
      - echo Build started on `date`
      - yarn run build
      - yarn run cdk -- synth

artifacts:
  base-directory: cdk.out
  files:
    - '**/*'